# GIT commands

## Initial setup

```
git config --global user.name "post-nov";
git config --global user.email svyatoslav.postnov@gmail.com;
git config --global core.editor vim;
git config --global alias.alias "! git config --get-regexp ^alias\. | sed -e s/^alias\.// -e s/\ /\ =\ /";
```

